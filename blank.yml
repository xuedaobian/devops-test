pipeline {
  agent any
  stages {
    stage('阶段-1') {
      steps {
        echo 'hello CODING'
      }
    }
    stage('阶段-2') {
      steps {
        junit 'target/surefire-reports/*.xml'
        npmAuditInDir(directory: '/', collectResult: true)
      }
    }
  }
}